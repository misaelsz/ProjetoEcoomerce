<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-warning">
        @Html.ActionLink("Ecommerce", "Index", "Home", new { id = 0 }, new { @class = "navbar-brand" })

        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                @*<li class="nav-item">
                    @Html.ActionLink("Cadastrar", "CadastrarPessoa",
                   "Home", new { @class = "nav-link" })
                </li>
                <li class="nav-item">
                    @Html.ActionLink("Listar", "ListarPessoas",
                   "Home", new { @class = "nav-link" })
                </li>*@
            </ul>
        </div>
    </nav>

    <div class="container" style="margin-top:50px;margin-bottom:50px; text-align:center">
        @RenderBody()
    </div>

    <hr />
    <footer style="text-align:center">
        <p>
            &copy; @DateTime.Now.Year - Desenvolvido por Diogo Deconto
        </p>
    </footer>
</body>
</html>
